doctrine:
    dbal:
        # configure these for your database server
        driver: 'pdo_sqlite'
        # url: '%env(resolve:DATABASE_URL)%'
        url: 'sqlite:///%kernel.project_dir%/var/360_test.db'